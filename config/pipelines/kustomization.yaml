namespace:  default

generatorOptions:
  disableNameSuffixHash: true
  
configMapGenerator:
  - name: maven-settings
    files:
      - settings.xml

resources:
  - sa.yaml
  - git-source-pvc.yaml
  - clusterrole.yaml
  - clusterrole-binding.yaml
  - fruits-api-pipeline.yaml
  